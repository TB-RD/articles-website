/*


/*<% const unique = [...new Set(urls.map(item => item.type))];%>
            <%= unique %> 
             <%= Array.from(new Set(urls.map((item) =>  item.type  ))) %>
            */

/*
    <% for(const data in urls) {%> 
    <ul>
        <li class="items">
            <%= urls[data].type %>
        </li>
    </ul>
    <% } %></div>*/


 /*   <% const letters = new Set(urls); %>
    <% letters.forEach (function(value) { %>
    <% let text = ""; %>
    <%=     value.type  %>
    <%- "<br>" %> 
    <%  }) %>*/

   /* <% let items = document.querySelectorAll(".items"); %>
<% items.addEventListener("click", () => { %>
<% console.log('aa') %>
        <% }) %> */

        /*
                <li class="folder1"> <% urls %> </li>
        <li class="folder2"> <% urls[0].name %> </li>
        <li class="folder3"> <% urls[0].name %> </li>
        <li class="folder4"> <% urls[0].name %> </li>
        <li class="folder5"> <% urls[0].name %> </li>
        <li class="folder6"> <% urls[0].name %> </li>
        
        */

            /*items.forEach(it => {
    it.addEventListener("click", () =>{
      enter.style.display = "block";
      name.style.display = "flex";
      }
    );
    //    const items = document.querySelectorAll(".items");
  })*/

  /*if(req.body.password === process.env.PASSWORD){
    console.log('yo');
    res.redirect('/');
  }else{
    console.log("hmm");
    res.redirect('login')
  }*/